version: 2

models:
  - name: stg_customers_tables

    columns:
      - name: customer_id
        tests:
          - unique
          - not_null

      - name: email
        tests:
          - not_null

      - name: status
        tests:
          - accepted_values:
              arguments:
                values:
                  - ACTIVE
                  - INACTIVE
